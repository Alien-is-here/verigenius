# VeriGenius — UVM Testbench Generation Pipeline
# Run this FIRST: pip install -r requirements.txt

# ── LangGraph (graph orchestration) ─────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0

# ── OpenAI-compatible client (for OpenRouter) ────────────────
openai>=1.0.0

# ── Read .docx spec document ─────────────────────────────────
python-docx>=1.1.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6